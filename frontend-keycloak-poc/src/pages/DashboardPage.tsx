import React, { FC } from 'react'
import { Layout } from 'src/templates'

const DashboardPage: FC = () => {
  return <Layout title="Dashboard Page">Dashboard Page</Layout>
}

export default DashboardPage
